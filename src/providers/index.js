/**
 * @module ProviderIndex Index for all React components used in the apps frontend
 * @exports (AppProvider) The provider for App context (React context provider)
 * @exports (AppContext) The App context (React context)
 */

export { default as AppProvider, AppContext } from "./AppProvider";